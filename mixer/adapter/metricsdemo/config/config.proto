syntax = "proto3";

// config for mygrpcadapter
package adapter.metricsdemo.config;

import "gogoproto/gogo.proto";

option go_package="config";

// config for metricsdemo
message Params {
    // Path of the file to save the information about runtime requests.
    // string file_path = 1;
}
